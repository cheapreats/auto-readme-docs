import styled from 'styled-components';

const Card = styled.div`
  display: inline-block;
  margin: 1rem 0rem;
  padding: 2rem;
  width: 100%;

  background: #212428;

  /* Shadow */
  box-shadow: 0px 28px 0px -14px rgba(0, 0, 0, 0.2);
`;

export default Card;
